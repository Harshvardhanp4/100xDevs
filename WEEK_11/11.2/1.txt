** CREATING NEW INSTANCE **

Go to ec2 -> Create New Instance

Give name-> Os (Windows) -> Instance Type-> t2.micro(for beginners)
-> Key Pair (for creating public and private key lets you connect to server directly(ssh into the machine))(RSA, .pem Create key pair it will download .pem file)
-> Check the following :_ 

Allow RDP traffic from
Helps you connect to your instance

Anywhere
0.0.0.0/0

Allow HTTPS traffic from the internet
To set up an endpoint, for example when creating a web server

Allow HTTP traffic from the internet
and done


To make backend run permanently 
do:-
 npm i -g pm2
 pm2 start index.js