1. Login to AWS Console → search for S3.

2. Create a bucket →

3. Choose region (e.g., ap-south-1).
keep it blocked if you’ll use CloudFront OAC (recommended).

4.Upload frontend build files → your compiled index.html, CSS, JS, images.
 If React: run npm run build → upload the build/ or dist folder.

5. Create a CloudFront distribution (not CloudWatch 😅 small correction):

Origin = your S3 bucket.

Use Origin Access Control (OAC) so bucket is private but CloudFront can fetch.

Copy the suggested bucket policy from CloudFront and paste into your S3 bucket policy.

Deploy → Wait until CloudFront status = Deployed.

Access via CDN URL → e.g., https://d123abcxyz.cloudfront.net/index.html.

You can later map your own domain via Route 53 or any DNS.